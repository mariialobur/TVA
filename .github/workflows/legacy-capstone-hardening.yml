name: Legacy capstone hardening
on:
  push:
    paths:
      - '.github/workflows/legacy-capstone-hardening.yml'
permissions:
  contents: write
jobs:
  harden:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - name: Apply legacy capstone hardening
        run: node tools/legacy-capstone-hardening.mjs
      - name: Commit hardened legacy modules
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add m04-tva-comptabilite-suisse-base.html course-legacy-gate.js
          git commit -m "Harden legacy written dossier gates" || exit 0
          git push
